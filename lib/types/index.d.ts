/**
 * dsh-hebrew-rtl — host half.
 *
 * The package contributes browser-side CSS and behaviour only; this entry
 * exists so the row inserted by cordis.patch.yml can resolve and mount the
 * package like any other plugin.
 */
export declare function apply(): void
