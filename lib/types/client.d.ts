/**
 * dsh-hebrew-rtl — client half.
 *
 * Loaded as a classic script by the DSH client module system, which it
 * self-registers with through `window.__ModuleLoader__.load`. The declaration
 * below describes the exports that handoff yields.
 */
export declare function apply(ctx: {
  effect(setup: () => () => void): void
}): void
